import { Event } from '../types/event';
import { Metric } from '../types/metric';
import { NetworkLayer } from '../types/networkLayer';
export declare const DEFAULT_NETWORK_LAYER: {
    sendMetrics: () => void;
    sendEvents: () => void;
};
export declare const scheduleEvent: (cb: () => void) => void;
export declare const sendEvents: (events: Event[]) => void;
export declare const sendMetrics: (metrics: Metric[]) => void;
export declare const createNetworkLayer: () => NetworkLayer;
