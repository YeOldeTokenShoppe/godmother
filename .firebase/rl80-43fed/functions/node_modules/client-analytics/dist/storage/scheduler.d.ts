import { CreateScheduler } from '../types/scheduler';
export declare const DEFAULT_SCHEDULER: {
    queue: import("../utils/queue").Queue<unknown>;
    add: () => undefined;
    items: never[];
    length: number;
};
export declare const createScheduler: CreateScheduler;
