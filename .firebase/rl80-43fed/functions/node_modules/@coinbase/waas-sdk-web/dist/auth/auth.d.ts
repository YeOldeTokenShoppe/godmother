import { LoginOptions, User } from "../index.js";
import { AuthMethod } from "./authMethod.js";
import { TokenPersistenceStrategy } from "./persistence/persistenceStrategy.js";
type SetupAuthOptions = {
    hostname: string;
    projectId?: string;
    verbose?: boolean;
};
export declare class AuthImpl {
    static hostname: string;
    static verbose: boolean;
    static provider?: AuthMethod;
    static projectId?: string;
    static persistenceStrategy: TokenPersistenceStrategy;
    static user?: User;
    static get api(): import("../api.js").Api;
    static setup(options: SetupAuthOptions): Promise<void>;
    static maybeRefresh(force: boolean): Promise<void>;
    static login(options?: LoginOptions): Promise<User>;
    static resume(): Promise<void>;
    private static resumeUser;
    static logout(): Promise<void>;
}
export {};
