import { TAuthToken } from "../util.js";
type CachedObject<T> = {
    contents: T;
    validUntil: Date;
};
export interface TokenPersistenceStrategy {
    get(): CachedObject<TAuthToken> | undefined;
    update(token: TAuthToken, ttlS: number): any;
    clear(): any;
}
export {};
