import { TAuthToken } from "../util.js";
import { TokenPersistenceStrategy } from "./persistenceStrategy.js";
export declare class LocalStoragePersistenceStrategy implements TokenPersistenceStrategy {
    token: TAuthToken | undefined;
    validUntil: Date | undefined;
    constructor();
    get(): {
        contents: TAuthToken;
        validUntil: Date;
    } | undefined;
    update(token: TAuthToken, ttlS: number): void;
    clear(): void;
}
