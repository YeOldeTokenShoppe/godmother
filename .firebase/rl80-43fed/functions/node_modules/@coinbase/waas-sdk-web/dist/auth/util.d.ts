export declare const DEFAULT_REAUTH_TIMEOUT: number;
export declare enum UserType {
    Unknown = 1,
    DevDelegatedAuth = 2,
    EndUserAuth = 3
}
export type TAuthToken = {
    authToken: string;
    refreshToken: string;
    endUserId?: string;
    userType?: UserType;
};
export declare const parseEncodedAuthToken: (encodedToken: string) => any;
