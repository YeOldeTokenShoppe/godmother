import { AuthMethod, TBaseOptions } from "../authMethod.js";
import { UserType } from "../util.js";
type TEUAOptions = {
    projectId: string;
};
export declare class EndUserAuthProvider extends AuthMethod {
    type: UserType;
    options: TEUAOptions & TBaseOptions;
    constructor(options: TEUAOptions & TBaseOptions);
    refresh(): Promise<void>;
    login(): Promise<void>;
    logout(): Promise<void>;
}
export {};
