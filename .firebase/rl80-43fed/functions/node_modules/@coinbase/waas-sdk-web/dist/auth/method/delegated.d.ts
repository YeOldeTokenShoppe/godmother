import { AuthMethod, TBaseOptions } from "../authMethod.js";
import { UserType } from "../util.js";
type TDelegatedOptions = {
    provideAuthToken: () => Promise<string>;
};
export declare class DelegatedAuthProvider extends AuthMethod {
    type: UserType;
    provideAuthToken: () => Promise<string>;
    constructor(options: TDelegatedOptions & TBaseOptions);
    login(): Promise<void>;
    refresh(): Promise<void>;
    fetchAndUpdateToken(): Promise<void>;
}
export {};
