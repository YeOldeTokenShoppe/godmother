export type WaasAuth = string;
type RequestType = {
    path: string;
    method: string;
    body: string | null;
};
type RequestHandler = (request: RequestType, meta: {
    service: string;
    method: string;
}) => Promise<unknown>;
export declare class HttpRequestHandler {
    #private;
    constructor(hostUrl: string, authToken?: WaasAuth | undefined, verbose?: boolean, correlationId?: string | undefined);
    requestHandler: RequestHandler;
}
export {};
