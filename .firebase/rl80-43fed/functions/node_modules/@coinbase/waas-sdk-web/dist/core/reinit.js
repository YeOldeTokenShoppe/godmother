var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import { createCWaaSInitServiceClient, } from "../generated/coinbase/cwaas_init/api/v1/index.js";
import { retryable, BaseClient } from "./baseclient.js";
export class WaaSInit extends BaseClient {
    path = "consumer/init";
    getClient() {
        const handler = this.getHandler();
        return createCWaaSInitServiceClient(handler.requestHandler);
    }
    async decryptSessionKey(sessionKeyHex) {
        const resp = await this.getClient().DecryptSessionKey({
            sessionKey: sessionKeyHex,
        });
        return resp.dek;
    }
}
__decorate([
    retryable()
], WaaSInit.prototype, "decryptSessionKey", null);
