import * as EndUserAuth from "../generated/coinbase/enduserauth/v1/index.js";
type TOptions = {
    hostname: string;
    authToken: string;
    verbose: boolean;
};
export declare class WaasEndUserAuth {
    #private;
    constructor(options: TOptions);
    refreshToken(refreshToken?: string): Promise<EndUserAuth.RefreshTokenResponse>;
    logOut(authToken?: string): Promise<EndUserAuth.LogOutResponse>;
}
export declare function unauthorizedEuaClient(): EndUserAuth.EndUserAuthService;
export {};
