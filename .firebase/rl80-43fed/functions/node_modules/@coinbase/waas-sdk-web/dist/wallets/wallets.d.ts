import { Wallet, BackupData, NewWallet, ProtocolFamily, HasApi, User } from "../index.js";
import { WalletImpl } from "./wallet.js";
import { InitializeWaasOptions } from "../waas.js";
export declare class WalletsImpl {
    static wallet?: WalletImpl;
    static apiProvider: HasApi;
    static options: InitializeWaasOptions;
    static get api(): import("../api.js").Api;
    static create(passcode?: string): Promise<NewWallet>;
    private static getLocalWalletCache;
    private static fetchWallet;
    private static activeMPCWallet;
    static resume(user: User): Promise<Wallet | null>;
    static restoreFromHostedBackup(passcode: string, protocolFamily?: ProtocolFamily): Promise<Wallet>;
    static restoreFromBackup(backup: BackupData, passcode?: string): Promise<Wallet>;
    static finalize(): void;
    private static getWallet;
    private static setWallet;
}
