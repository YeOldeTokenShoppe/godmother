export declare enum ProtocolFamily {
    EVM = "protocolFamilies/evm"
}
export type IProtocolFamily = {
    [K in keyof ProtocolFamily]: ProtocolFamily[K] extends string ? ProtocolFamily[K] : never;
}[keyof ProtocolFamily];
